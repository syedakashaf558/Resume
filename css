/* General body styling */
body {
    font-family: 'Arial', sans-serif;
    background-color: #f7f7f7;
    color: #2c3e50;
    margin: 0;
    padding: 0;
}

/* Container for the resume */
.container {
    max-width: 800px;
    margin: 20px auto;
    padding: 20px;
    background-color: #ffffff;
    box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
    border-radius: 10px;
    transition: box-shadow 0.3s ease;
}

/* Hover effect for the container */
.container:hover {
    box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.2);
}

/* Header styling */
h1 {
    text-align: center;
    color: #2980b9;
    margin-top: 0;
}

/* Section headers */
h2 {
    color: #e74c3c;
    border-bottom: 2px solid #e74c3c;
    padding-bottom: 5px;
    margin-bottom: 20px;
}

/* Paragraphs and list items */
p, li {
    margin: 10px 0;
    color: #34495e;
}

/* List styling */
ul {
    list-style-type: square;
    padding-left: 20px;
}

/* List item transitions and background */
ul li {
    background-color: #ecf0f1;
    padding: 10px;
    margin: 5px 0;
    border-radius: 5px;
    transition: background-color 0.3s ease, transform 0.3s ease;
}

/* List item hover effects */
ul li:hover {
    background-color: #bdc3c7;
    transform: scale(1.05);
}

/* Styling for labels */
b {
    color: #16a085;
}

/* Sections styling */
section {
    margin-bottom: 40px;
}

/* Different background colors for sections */
#work-experience {
    background-color: #f9ebea;
    padding: 20px;
    border-radius: 10px;
}

#education {
    background-color: #eafaf1;
    padding: 20px;
    border-radius: 10px;
}

#certificates, #accomplishments, #skills, #referees {
    background-color: #f4f6f6;
    padding: 20px;
    border-radius: 10px;
}
